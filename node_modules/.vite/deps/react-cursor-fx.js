import {
  require_react
} from "./chunk-UGC3UZ7L.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/react-cursor-fx/dist/index.esm.js
var import_react = __toESM(require_react());
var isValidUrl = (url) => {
  try {
    new URL(url, window.location.origin);
    return true;
  } catch (e) {
    return false;
  }
};
var Cursor = ({ type = "default", size = 20, imageUrl, targetElement }) => {
  const defaultRef = (0, import_react.useRef)(document.body);
  const previousStyle = (0, import_react.useRef)("");
  (0, import_react.useEffect)(() => {
    let target = null;
    if (targetElement) {
      if (typeof targetElement === "string") {
        target = document.querySelector(targetElement);
      } else if (targetElement.current) {
        target = targetElement.current;
      }
    } else {
      target = defaultRef.current;
    }
    if (!target) {
      return;
    }
    previousStyle.current = target.style.cursor || "auto";
    let cursorStyle = type;
    if (type === "custom" && imageUrl) {
      if (isValidUrl(imageUrl)) {
        const resolvedUrl = new URL(imageUrl, window.location.origin).href;
        cursorStyle = `url(${resolvedUrl}) ${size / 2} ${size / 2}, auto`;
      } else {
        cursorStyle = "auto";
      }
    }
    target.style.cursor = cursorStyle;
    return () => {
      target.style.cursor = previousStyle.current;
    };
  }, [type, size, imageUrl, targetElement]);
  return null;
};
export {
  Cursor
};
//# sourceMappingURL=react-cursor-fx.js.map
